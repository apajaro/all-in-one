package com.apajaro.platform.domain;

public class UserLoginTest {
    /*@Test
    @DisplayName("Test successful login")
    void testFailedLogin() {
        UserLogin userLogin = TestUtils.makeUserLogin();
        userLogin.failedToLogin();
        userLogin.failedToLogin();
        userLogin.failedToLogin();


        Assertions.assertEquals(3, userLogin.getSuccessiveFailedLogins());
        Assertions.assertFalse(userLogin.isEnabled());
        Assertions.assertNotNull(userLogin.getDisabledDateTime());
    }*/
}
