package com.apajaro.platform.service;

import com.apajaro.platform.application.repository.*;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.mockito.Mockito.spy;

@ExtendWith(MockitoExtension.class)
public class AuthServiceTest {
    /*@Mock
    private UserLoginRepository userLoginRepository;
    @Mock
    private UserLoginTokenRepository userLoginTokenRepository;
    @Mock
    private UserLoginPasswordResetRepository userLoginPasswordResetRepository;
    @Mock
    private SecurityPermissionRepository securityPermissionRepository;
    @Mock
    private PersonRepository personRepository;
    @Mock
    private PasswordEncoder passwordEncoder;
    @Mock
    private EmailService emailService;

    private AuthService authService;

    @BeforeEach
    void setUp() {
        authService = spy(new AuthServiceImpl(userLoginRepository, userLoginTokenRepository, userLoginPasswordResetRepository, securityPermissionRepository, personRepository, passwordEncoder, emailService));
    }*/
}
